export const setLoding = (value) => {
  return { type: "SET_LOADING", value };
};

export const IsLogin = (value) => {
  return { type: "ISLOGIN", value };
};

export const IsLogout = (value) => {
  return { type: "ISLOGOUT", value };
};
